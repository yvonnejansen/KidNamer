// Strings for the button
var suggestion_string = "Get a suggestion";
var scroll_string = "Go to name ↓";
var add_string = "Add name";
var preferred_name_string = "Preferred name";

// Valid login options. These need to correspond to the ones set in index.php!
var login_options = ["test", "test2"];

// How many names you want to have appear in bold font at the top.
var nbest = 5;

// If you don't have a dataset from which to suggest names, i.e., the table "name_table" then set this to false.
var use_randomNames = true;

// If you don't have data for ranks, then turn off the function here to avoid javascript errors.
var use_ranks = true;
